import { Text, View } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context";

const Ayat = () => {
    return (
        <SafeAreaView>
            <Text>Ayat</Text>
        </SafeAreaView>
    );
};

export default Ayat;